# mysql-select-queries-task3
This repository contains SQL SELECT query practice for Task 3 using MySQL Workbench. It includes SELECT, WHERE, AND/OR, LIKE, BETWEEN, ORDER BY, and LIMIT queries written for the students, courses, and enrollments tables. All examples are organized in a single SQL script.
